const step1 = {
  // Get selectors
  // add event Listeners. video has four. 'click' 'pause' 'play' 'timeupdate'
  // Need to also add event listeners for play, stop and progress
};

const step2 {
  // add function that stops and starts video from the video and play button
  // add function that updates play icon
  // Add function that stops video
  // hard - function to update progress bar. It also updates seconds and minutes for timestamp
  // one line function to set current video time.
}
